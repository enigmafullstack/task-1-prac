a shell is a program that provides an interface for users to interact with the operating system. It takes commands from the user and gives them to the operating system to execute. The shell can be accessed through a command-line interface (CLI) or a graphical user interface (GUI)

I learned how to confidently perform daily tasks in the command line environment. 
